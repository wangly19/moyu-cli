{{
  request
}}

{{response}}

export default function {{name}} <
  P = {{ request ? requestName : "any" }},
  R = {{ response ? responseName : "any" }}
>(
  params: P,
  options?: Record<string, any>
): Promise<R>